{
  "name": "exam-boilerplate",
  "version": "1.0.0",
  "description": "Generic boilerplate for exam experiments",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "private": true
}

